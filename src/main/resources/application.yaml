server.port: 8080

spring:
  application:
    name: FilsoftCreditSolutionProject

  datasource:
    # Inlocuieste [YOUR-PASSWORD] cu parola ta din Supabase
    url: jdbc:postgresql://aws-0-eu-west-1.pooler.supabase.com:5432/postgres
    username: postgres.kcotyswoxnackezhsdoy
    password: Mate102030405060708090!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      # validate | update | create | create-drop | none
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
